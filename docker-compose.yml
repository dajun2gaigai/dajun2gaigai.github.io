services:
  jekyll:
    build: .
    container_name: jekyll-site
    ports:
      - "8080:8080"
    volumes:
      - .:/srv/jekyll
    environment:
      - JEKYLL_ENV=development
    command: /tmp/entry_point.sh
    stdin_open: true
    tty: true
